# Operation index

_Auto-generated by `ws1-build codegen-skill` at 2026-04-30T05:40:06Z. Do not edit by hand; rerun the maintainer pipeline after spec changes._

Total operations: **7** across **2** section(s).

Columns:
- **Op identifier**: canonical name to pass to `ws1 ops describe`.
- **Class**: `read` / `write` / `destructive`. Destructive ops always require browser approval.
- **Reversible**: `full` / `partial` / `none` / `unknown`. Affects how confidently an agent should propose the op.
- **Sync**: `sync` returns a result inline; `async` returns a `job_id` to poll.
- **Identifier**: argv shape needed to target the op (`device_id`, `device_id_array`, etc.).
- **Summary**: one-line description from the spec.

## mdmv4

| Op identifier | Class | Reversible | Sync | Identifier | Summary |
|---|---|---|---|---|---|
| `mdmv4.devices.bulkcommand` | write | depends_on_command | sync | device_id_array | Bulk command on devices |
| `mdmv4.devices.get` | read | — | sync | device_id | Get device by ID |
| `mdmv4.devices.lock` | write | full | sync | device_id | Lock device |
| `mdmv4.devices.search` | read | — | sync | none | Search devices |
| `mdmv4.devices.wipe` | destructive | none | async | device_id | Wipe device (enterprise) |

## systemv2

| Op identifier | Class | Reversible | Sync | Identifier | Summary |
|---|---|---|---|---|---|
| `systemv2.users.get` | read | — | sync | user_id | Get user by ID |
| `systemv2.users.search` | read | — | sync | none | Search enrolled users |

