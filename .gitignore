# Build output
/bin/
/.build/
/dist/
*.test
*.out
coverage.out

# Local config / runtime state (must never be checked in)
/tmp/
*.local.yaml
.env
.env.*

# Agent-instruction config — local only, never check in.
CLAUDE.md
GEMINI.md
AGENTS.md
.claude/

# Internal maintainer docs — not for public repo.
# (Superpowers brainstorming + planning docs, internal pipelines.)
/docs/superpowers/
/docs/build-pipeline.md
/docs/spec-acquisition.md

# IDE / editor
.idea/
.vscode/
*.swp
.DS_Store

# Dependency & toolchain caches
/vendor/
